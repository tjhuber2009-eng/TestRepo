import {copyFile,mkdir} from 'node:fs/promises';import {execFileSync} from 'node:child_process';import {join} from 'node:path';
let triple=process.env.TAURI_ENV_TARGET_TRIPLE||'';if(!triple){try{triple=execFileSync('rustc',['--print','host-tuple'],{encoding:'utf8'}).trim()}catch{try{triple=/^host:\s+(\S+)/m.exec(execFileSync('rustc',['-vV'],{encoding:'utf8'}))?.[1]||''}catch{}}}if(!triple)throw new Error('Rust is required to determine the Tauri target triple.');const ext=process.platform==='win32'?'.exe':'';await mkdir(new URL('../src-tauri/binaries/',import.meta.url),{recursive:true});await copyFile(process.execPath,new URL(`../src-tauri/binaries/yke-node-${triple}${ext}`,import.meta.url));console.log(`Prepared bundled Node sidecar for ${triple}`);
