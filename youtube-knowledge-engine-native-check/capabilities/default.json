{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-capability",
  "description": "Only the bundled provider-settings window can read or update provider configuration.",
  "windows": ["settings"],
  "permissions": [
    "allow-desktop-provider-status",
    "allow-desktop-save-provider-settings"
  ]
}
