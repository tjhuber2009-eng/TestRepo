name: Indicator Unbiased Bootstrap DD Frontier

on:
  push:
    branches:
      - indicator-long-validation-20260903
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy yfinance
      - name: Run unbiased block-bootstrap DD frontier
        run: python indicator_validation/run_unbiased_bootstrap.py
      - name: Hash outputs
        run: sha256sum indicator_validation/run_unbiased_bootstrap.py indicator_validation/output_unbiased_bootstrap/* > indicator_validation/SHA256SUMS_UNBIASED_BOOTSTRAP.txt
      - name: Upload unbiased bootstrap artifact
        uses: actions/upload-artifact@v4
        with:
          name: indicator-unbiased-bootstrap-frontier
          path: |
            indicator_validation/output_unbiased_bootstrap/
            indicator_validation/SHA256SUMS_UNBIASED_BOOTSTRAP.txt
          if-no-files-found: error
          retention-days: 7
